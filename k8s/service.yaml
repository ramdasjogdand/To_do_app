apiVersion: v1
kind: Service
metadata:
  name: to-do-app-service
spec:
  type: NodePort
  selector:
    app: to-do-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30007
